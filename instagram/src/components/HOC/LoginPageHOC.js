import React from 'react';

const LoginPageHOC = WrappedComponent => {
    return class extends React.Component {
        render(
            if (this.props.)
        )
    }
}

export default LoginPageHOC;