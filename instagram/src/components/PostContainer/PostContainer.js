

function PostContainer(props) {
    return(
        <div>
            {props.dummyData.map(data
                <dummyData key = {data.username} data={username} />
            )}
        </div>
    )
}

export default PostContainer;