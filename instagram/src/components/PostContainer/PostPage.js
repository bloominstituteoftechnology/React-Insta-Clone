import React from 'react';

const PostPage = post => {
    return (
        class extends React.Component {
            constructor() {
                super();
                this.state = {};
            }
        }
    );
};

export default PostPage;