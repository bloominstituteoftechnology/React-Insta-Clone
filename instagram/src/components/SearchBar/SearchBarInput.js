import React from 'react';

function SearchBarInput(props) {
    return (
      <form>
          <input
            type = "text"
            name = "search"
            placeholder = "search"
          />
      </form>
    );
  }

export default SearchBarInput;