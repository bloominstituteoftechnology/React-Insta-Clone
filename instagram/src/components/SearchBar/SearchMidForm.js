import React from "react";

const SearchMidForm = props => {
  return (
    <div className="form-group">
      <input
        type="search"
        className="form-control-lg"
        id="search-input"
        placeholder="Search"
      />
    </div>
  );
};

export default SearchMidForm;
