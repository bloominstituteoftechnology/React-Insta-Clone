import React from 'react';

const SearchBar = (props) => {
        return(
                <div>
                        <h1>This is a search bar.</h1>
                </div>
        )
}

export default SearchBar;