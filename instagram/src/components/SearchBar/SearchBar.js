import React from "react";
import NavLogo from "./NavLogo";

const SearchBar = () => {
  return (
    <div>
      {/* Logo component */}
      <NavLogo />
      {/* Search component */}
      {/* Icons component */}
    </div>
  );
};

export default SearchBar;
