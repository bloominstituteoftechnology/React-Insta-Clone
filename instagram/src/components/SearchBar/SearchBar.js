import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <form action="">
        <input type="text" placeholder="Search" />
      </form>
    </div>
  );
};

export default SearchBar;
