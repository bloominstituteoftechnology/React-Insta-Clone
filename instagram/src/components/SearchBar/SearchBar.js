import React, { Component } from 'react';
import './SearchBar.css';

const SearchBar = props => {
    return <div className='search-bar-container'>SearchBar is showing</div>
}

export default SearchBar;