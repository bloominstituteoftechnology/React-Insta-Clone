import React from 'react';

const SearchBar = props => {
    return (
        <div>
            <form>
                SEARCH INPUT FIELD:
                <input type = "text" />
            </form>
        </div>
    );
}

export default SearchBar;