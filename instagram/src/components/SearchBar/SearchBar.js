import React from 'react';

const SearchBar = props => {
  return (
    <div className="search-main">
      <input className="search-bar" placeholder="search bar"></input>
    </div>
  )
}

export default SearchBar;
