import React from 'react';

const SearchBarActionsExploreButton = (props) => {
  return(
    <button className="search-bar-actions__explore-button">
    </button>
  );
};

export default SearchBarActionsExploreButton;