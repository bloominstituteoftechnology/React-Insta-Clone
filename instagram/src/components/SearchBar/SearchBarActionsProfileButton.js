import React from 'react';
import './SearchBar.css';

const SearchBarActionsProfileButton = (props) => {
  return(
    <button className="search-bar-actions__profile-button">
    </button>
  );
};

export default SearchBarActionsProfileButton;