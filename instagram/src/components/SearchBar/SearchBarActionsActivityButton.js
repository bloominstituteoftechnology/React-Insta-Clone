import React from 'react';
import './SearchBar.css';

const SearchBarActionsActivityButton = (props) => {
  return(
    <button className="search-bar-actions__activity-button">
    </button>
  );
};

export default SearchBarActionsActivityButton;