import React from 'react'

const SearchBar = props => {
  return(
    <div>
      <input 
        type="text"
        name="searchbar"
        placeholder="search"
      />
    </div>
  )
}

export default SearchBar