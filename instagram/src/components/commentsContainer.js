import React from 'react';
import '../App.css';

export const commentsContainer = ({ comments }) => {

  return (
    <ul>
        {postData.comments.map((comments, i) => {
          return (
            
          );
        })}
    </ul>
  );
};
