import styled, { css } from 'styled-components' ;

export const SearchBarStyled = styled.div`
    background-color: grey ;
    // ${props => props.myHeight && css`
    //     height: 50px;
    // `};
`;
