import React from 'react';

const Login = (props) => {

    return (

        <div>
            <form onSubmit={}>
                <input type='text' placeholder='Login'/>
                <input type='text' placeholder="Password"/>
                <button>Login</button>
            </form>
        </div>
    )
}