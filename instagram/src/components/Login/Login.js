import React from 'react';
import './login.css';

const Login = (props) => {
    return (
        <form >
            <input placeholder=" Username..." />
            <input placeholder=" Password..." />
            <button />
        </form>
    )
}

export default Login;