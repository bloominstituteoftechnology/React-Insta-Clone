import React from 'react'

const Login = props => {
    return (
        <form>
            <input placeholder = 'username' />
            <input placeholder = 'password' />
            <button>Login</button>
        </form>
        
    )
}

export default Login