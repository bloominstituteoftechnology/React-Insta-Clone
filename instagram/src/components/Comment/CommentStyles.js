import styled from 'styled-components';

export const CommentContainer = styled.div`
  padding: 5px 0 6px;
`;

export const CommentUsername = styled.a`
  margin: 0;
  font-size: 1rem;
  font-weight: bold;
  margin-right: 4px;
`;

export const CommentText = styled.span`
  margin: 0;
  font-size: 1rem;
`;
