import React from 'react';

const CommentInput = () => {
  return (
    <input
      type="text"
      className="CommentInput"
      placeholder="Add a comment..."
    />
  );
};

export default CommentInput;
