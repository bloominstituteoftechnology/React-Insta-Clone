import React from 'react';

const CommentInput = props => {
    return (
        <form>
            <input type="text" placeholder="Add coment..." className="comment-input" />
        </form>
    );
};

export default CommentInput;