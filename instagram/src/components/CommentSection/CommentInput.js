import React from 'react';

const CommentInput = () => {
    return ( 
        <div>
            <input type="text" placeholder="Add a comment..." />
        </div>
    );
}

export default CommentInput;