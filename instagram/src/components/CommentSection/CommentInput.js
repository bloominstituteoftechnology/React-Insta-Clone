import React from 'react';

function PostImg(props) {
    return (
        <form>
            <input
              type = "text"
              name = "comment"
              placeholder = "Add a comment..."
            />
        </form>
      );
}

export default PostImg;