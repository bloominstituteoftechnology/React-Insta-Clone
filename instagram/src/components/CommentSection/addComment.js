import React from 'react';

const AddComment = (props) => {
    return(
        <div>
        {/* {props.username}
        {props.text} */}
        <div>
            {props.username}
            {props.text}
        </div>
        
        </div>
    )
}

export default AddComment