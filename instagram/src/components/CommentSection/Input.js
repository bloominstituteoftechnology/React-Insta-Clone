import React from "react";

const Input = props => {
    return(
        <form>
            <input type='text' placeholder='What are your thoughts...' />
        </form>
    );
}

export default Input;