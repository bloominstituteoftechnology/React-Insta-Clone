import React from 'react';

const Comment = props => {
    return(
        <div className='Comment'>
            <span>{props.comment.username}</span>
            <span>{props.comment.username}</span>
        </div>
    );
}

export default Comment;