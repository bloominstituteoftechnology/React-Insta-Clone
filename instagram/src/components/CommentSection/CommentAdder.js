import React from 'react';
import './Comment.css';

const CommentAdder = (props) => {
    return <input type = 'text' className = 'comment-adder' placeholder = 'Add a comment...'/>;
    
}

export default CommentAdder;