import styled from 'styled-components';

const Username = styled.span`
    font-weight: 500;
    font-size: 1.0rem;
    align-self: center;
    cursor: pointer;
    margin-right: 10px;
    color: grey;
`;

export default Username;