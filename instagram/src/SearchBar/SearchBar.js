import React from 'react';

const SearchBar = (props) => {
    render () {
        return (
            <>
            Logo
            Search Bar Input
            Icons
            </>
        )
    }
}

export default SearchBar;