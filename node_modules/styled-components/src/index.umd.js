// @flow
import * as lib from './'

const { default: styled, ...rest } = lib

// if you don't do this then the browser build has to be used like window.styled.default``
for (const key in rest) {
  if (rest.hasOwnProperty(key)) styled[key] = rest[key]
}

export default styled
