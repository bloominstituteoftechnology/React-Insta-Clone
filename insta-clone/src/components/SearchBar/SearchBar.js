import React from 'react';

const SearchBar = (props) => {
    return(
        <div className="search-bar">
            <p>SearchBar will be here</p>
        </div>
    );
}

export default SearchBar;