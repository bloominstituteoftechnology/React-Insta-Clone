import React from 'react';
import './PostContainer.css';

const Header = (props) => {
    return (
        <div>
            <img />
            <p>username</p>
        </div>
    );
};

export default Header;