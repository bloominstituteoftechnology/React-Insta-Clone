import React from 'react';

const SearchBar = props => {
     return (
         <div className='searchBar'>
         <input className="search" placeholder="Search" />
         </div>
     )
}

export default SearchBar;

//nav component icon ig name search bar and icons
// 3 divs 