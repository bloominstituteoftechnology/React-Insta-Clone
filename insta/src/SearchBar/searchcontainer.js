import React from 'react';
import fontawesome from '@fortawesome/fontawesome';
import Search from './search'
const SearchContainer = (props) => {
    return (
        <div>
            CameraIcon | Instagram <Search /> Compass Heart User
        </div>
    );
};

export default SearchContainer;